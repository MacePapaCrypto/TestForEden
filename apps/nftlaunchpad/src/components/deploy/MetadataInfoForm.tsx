import { Container } from '@mui/material';
import React from 'react';

const MetadataInfoForm = (props = {}) => {
    return (
        <h1>This is the metadata info page</h1>
    );
}

export default MetadataInfoForm;