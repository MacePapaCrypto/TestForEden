import { Container } from '@mui/material';
import React from 'react';

const ImageInfoForm = (props = {}) => {
    return (
        <h1>This is the image info page</h1>
    );
}

export default ImageInfoForm;